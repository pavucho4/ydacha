{
  "name": "ydacha",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm ci && npm run build && ls -la client/build",
    "postinstall": "cd client && npm ci && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5"
  }
}